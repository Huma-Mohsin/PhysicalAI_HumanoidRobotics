---
id: week-12-13-conversational
title: "ہفتہ 12-13: VLA کے ساتھ گفتگو کرنے والی روبوٹکس"
sidebar_label: "ہفتہ 12-13: گفتگو AI"
sidebar_position: 7
---

import PersonalizeButton from '@site/src/components/Personalization/PersonalizeButton';
import ContentVariant from '@site/src/components/Personalization/ContentVariant';


# ہفتہ 12-13: Vision-Language-Action ماڈلز کے ساتھ گفتگو کرنے والی روبوٹکس

## جائزہ

**توجہ**: VLA (Vision-Language-Action) ماڈلز استعمال کرتے ہوئے آواز سے کنٹرول ہونے والے ہیومنائیڈ روبوٹس

اس آخری ماڈیول میں، آپ ہر چیز کو یکجا کر کے ایک گفتگو کرنے والا ہیومنائیڈ روبوٹ بنائیں گے جو:
- آواز کے احکامات سنتا ہے (Whisper ASR)
- بصارت اور زبان استعمال کرتے ہوئے سیاق و سباق سمجھتا ہے (Gemini VLA)
- قابل عمل روبوٹ ایکشنز بناتا ہے (ROS 2 احکامات)
- سمیولیشن اور حقیقی ہارڈویئر میں محفوظ طریقے سے حرکتیں انجام دیتا ہے

---

## سیکھنے کے مقاصد

اس ماڈیول کے اختتام تک، آپ یہ کر سکیں گے:

1. ✅ حقیقی وقت میں تقریر کی شناخت کے لیے Whisper کو ضم کرنا
2. ✅ ایکشن پلاننگ کے لیے Google Gemini کو VLA ماڈل کے طور پر جوڑنا
3. ✅ قدرتی زبان کو روبوٹ ایکشنز میں تبدیل کرنا
4. ✅ آواز سے کنٹرول ہونے والا نیویگیشن سسٹم بنانا
5. ✅ ہارڈویئر ایکسیلریشن کے ساتھ Jetson پر ڈیپلائے کرنا
6. ✅ Isaac Sim میں ٹیسٹ کرنا اور حقیقی روبوٹ میں منتقل کرنا

---

<PersonalizeButton />

## VLA پائپ لائن

```
┌──────────────┐
│ Voice Input  │  "Pick up the red cube"
└──────┬───────┘
       │
┌──────▼───────┐
│   Whisper    │  Speech → Text
└──────┬───────┘
       │
┌──────▼───────┐
│ Gemini VLA   │  Text + Vision → Action Plan
└──────┬───────┘
       │
┌──────▼───────┐
│ ROS 2 Action │  Execute motion
└──────────────┘
```

---

## حصہ 1: Whisper کے ساتھ تقریر کی شناخت

OpenAI کا Whisper ایک مضبوط تقریر شناخت ماڈل ہے جو 99 زبانوں کو سپورٹ کرتا ہے۔

### Whisper انسٹال کرنا

<ContentVariant hardwareType="gpu_workstation">

#### GPU ورک سٹیشن صارفین کے لیے (RTX 4090+)

**GPU-Accelerated انسٹالیشن**

```bash
# PyTorch کو CUDA کے ساتھ انسٹال کریں
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Whisper انسٹال کریں
pip install openai-whisper

# آڈیو پروسیسنگ انسٹال کریں
sudo apt-get install ffmpeg
pip install pyaudio

# GPU دستیابی ٹیسٹ کریں
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
```

**کارکردگی**: RTX 4090 پر `whisper-large-v3` کے ساتھ حقیقی وقت میں ٹرانسکرپشن۔

</ContentVariant>

<ContentVariant hardwareType="edge_device">

#### Jetson Orin Nano صارفین کے لیے

**Jetson-Optimized انسٹالیشن**

```bash
# Jetson کے لیے PyTorch انسٹال کریں (ARM64)
pip install torch torchvision torchaudio

# Whisper انسٹال کریں (Jetson کے لیے چھوٹا ماڈل استعمال کریں)
pip install openai-whisper

# آڈیو لائبریریز انسٹال کریں
sudo apt-get install portaudio19-dev python3-pyaudio
pip install pyaudio

# حقیقی وقت کی کارکردگی کے لیے whisper-base یا whisper-small استعمال کریں
```

**تجویز**: Jetson پر 2-3x تیز تر انفرنس کے لیے `whisper-base` ماڈل استعمال کریں۔

</ContentVariant>

<ContentVariant hardwareType="cloud_mac">

#### Cloud / Mac صارفین کے لیے

**CPU انسٹالیشن**

```bash
# PyTorch انسٹال کریں (CPU ورژن)
pip install torch torchvision torchaudio

# Whisper انسٹال کریں
pip install openai-whisper

# Mac: آڈیو لائبریریز انسٹال کریں
brew install portaudio
pip install pyaudio

# CPU کے لیے whisper-tiny یا whisper-base استعمال کریں
```

**متبادل**: مقامی GPU کے بغیر تیز تر انفرنس کے لیے Google Cloud Speech-to-Text API استعمال کریں۔

</ContentVariant>

---

### حقیقی وقت میں تقریر کی شناخت

```python
import whisper
import pyaudio
import wave
import tempfile

class VoiceListener:
    def __init__(self, model_size="base"):
        """
        model_size: tiny, base, small, medium, large
        GPU صارفین: بہترین درستگی کے لیے 'large-v3' استعمال کریں
        Jetson صارفین: حقیقی وقت کی کارکردگی کے لیے 'base' استعمال کریں
        """
        self.model = whisper.load_model(model_size)
        self.audio = pyaudio.PyAudio()

    def record_audio(self, duration=5):
        """مائیکروفون سے آڈیو ریکارڈ کریں"""
        CHUNK = 1024
        FORMAT = pyaudio.paInt16
        CHANNELS = 1
        RATE = 16000

        stream = self.audio.open(
            format=FORMAT,
            channels=CHANNELS,
            rate=RATE,
            input=True,
            frames_per_buffer=CHUNK
        )

        print("🎤 سن رہا ہے...")
        frames = []

        for _ in range(0, int(RATE / CHUNK * duration)):
            data = stream.read(CHUNK)
            frames.append(data)

        stream.stop_stream()
        stream.close()

        # عارضی WAV فائل میں محفوظ کریں
        with tempfile.NamedTemporaryFile(suffix=".wav", delete=False) as f:
            wf = wave.open(f.name, 'wb')
            wf.setnchannels(CHANNELS)
            wf.setsampwidth(self.audio.get_sample_size(FORMAT))
            wf.setframerate(RATE)
            wf.writeframes(b''.join(frames))
            wf.close()
            return f.name

    def transcribe(self, audio_file):
        """آڈیو کو متن میں تبدیل کریں"""
        result = self.model.transcribe(audio_file)
        return result["text"]

    def listen_and_transcribe(self, duration=5):
        """ایک کال میں ریکارڈ اور ٹرانسکرائب کریں"""
        audio_file = self.record_audio(duration)
        text = self.transcribe(audio_file)
        print(f"📝 ٹرانسکرائب شدہ: {text}")
        return text

# استعمال
listener = VoiceListener(model_size="base")
command = listener.listen_and_transcribe(duration=5)
print(f"صارف نے کہا: {command}")
```

---

## حصہ 2: Google Gemini کے ساتھ VLA

Google Gemini بصارت اور زبان کو یکجا کر کے روبوٹ ایکشنز بناتا ہے۔

### Gemini API سیٹ اپ کرنا

```bash
# Gemini SDK انسٹال کریں
pip install google-generativeai

# API key سیٹ کریں
export GEMINI_API_KEY="your-api-key-here"
```

**اپنی API key حاصل کریں**: https://makersuite.google.com/app/apikey

---

### VLA Prompt انجینئرنگ

```python
import google.generativeai as genai
import os

class VLAController:
    def __init__(self):
        genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
        self.model = genai.GenerativeModel('gemini-1.5-pro')

        # VLA کے لیے سسٹم prompt
        self.system_prompt = """You are a humanoid robot control system.
Convert natural language commands into ROS 2 action sequences.

Available actions:
- navigate(x, y, theta): Move to position (x, y) with orientation theta
- grasp(object_name): Pick up an object
- place(x, y, z): Place object at position
- wave(): Wave hand
- turn(degrees): Rotate in place
- speak(text): Say something

Output format (JSON):
{
  "actions": [
    {"type": "navigate", "params": {"x": 2.0, "y": 1.0, "theta": 0.0}},
    {"type": "grasp", "params": {"object": "red_cube"}}
  ],
  "explanation": "Brief explanation of the plan"
}

Only output valid JSON. No other text."""

    def plan_actions(self, command, image=None):
        """
        آواز کے حکم کو ایکشن سیکوینس میں تبدیل کریں

        Args:
            command: قدرتی زبان کا حکم (Whisper سے)
            image: اختیاری کیمرہ تصویر (PIL Image یا فائل پاتھ)
        """
        prompt = f"{self.system_prompt}\n\nUser command: {command}"

        if image:
            # Multimodal input (متن + تصویر)
            response = self.model.generate_content([prompt, image])
        else:
            # صرف متن input
            response = self.model.generate_content(prompt)

        return response.text

# استعمال
vla = VLAController()

# صرف متن کا حکم
command = "Go to the kitchen and pick up the red mug"
actions = vla.plan_actions(command)
print(actions)

# بصارت کے ساتھ (multimodal)
from PIL import Image
camera_image = Image.open("camera_feed.jpg")
command = "Pick up the object in front of me"
actions = vla.plan_actions(command, image=camera_image)
print(actions)
```

**متوقع آؤٹ پٹ:**

```json
{
  "actions": [
    {"type": "navigate", "params": {"x": 5.0, "y": 3.0, "theta": 1.57}},
    {"type": "grasp", "params": {"object": "red_mug"}}
  ],
  "explanation": "Navigate to kitchen coordinates, then grasp the red mug"
}
```

---

## حصہ 3: ROS 2 کے ساتھ ایکشن اجرائی

JSON ایکشنز کو ROS 2 احکامات میں تبدیل کریں۔

```python
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import PoseStamped
from nav2_msgs.action import NavigateToPose
from rclpy.action import ActionClient
import json

class ActionExecutor(Node):
    def __init__(self):
        super().__init__('action_executor')

        # Navigation action client
        self.nav_client = ActionClient(self, NavigateToPose, 'navigate_to_pose')

    def execute_actions(self, actions_json):
        """VLA سے ایکشن سیکوینس انجام دیں"""
        actions = json.loads(actions_json)

        for action in actions['actions']:
            action_type = action['type']
            params = action['params']

            if action_type == 'navigate':
                self.navigate_to(params['x'], params['y'], params['theta'])
            elif action_type == 'grasp':
                self.grasp_object(params['object'])
            elif action_type == 'wave':
                self.wave_hand()
            # مزید ایکشنز شامل کریں...

    def navigate_to(self, x, y, theta):
        """Nav2 استعمال کرتے ہوئے پوزیشن پر جائیں"""
        goal_msg = NavigateToPose.Goal()
        goal_msg.pose.header.frame_id = 'map'
        goal_msg.pose.pose.position.x = x
        goal_msg.pose.pose.position.y = y

        # theta کو quaternion میں تبدیل کریں
        from tf_transformations import quaternion_from_euler
        q = quaternion_from_euler(0, 0, theta)
        goal_msg.pose.pose.orientation.z = q[2]
        goal_msg.pose.pose.orientation.w = q[3]

        self.get_logger().info(f'({x}, {y}, {theta}) پر جا رہے ہیں')
        self.nav_client.send_goal_async(goal_msg)

    def grasp_object(self, object_name):
        """چیز پکڑیں (آسان بنایا گیا)"""
        self.get_logger().info(f'{object_name} کو پکڑ رہے ہیں')
        # یہاں پکڑنے کی منطق لاگو کریں
        # - بازو کو pre-grasp pose میں لے جائیں
        # - گرپر بند کریں
        # - چیز اٹھائیں

    def wave_hand(self):
        """ہاتھ ہلانے کا اشارہ"""
        self.get_logger().info('ہاتھ ہلا رہے ہیں')
        # ہاتھ ہلانے کی حرکت کے لیے joint trajectory شائع کریں

# مین لوپ
def main():
    rclpy.init()

    # اجزاء کو شروع کریں
    listener = VoiceListener(model_size="base")
    vla = VLAController()
    executor = ActionExecutor()

    print("🤖 گفتگو کرنے والا روبوٹ تیار ہے!")

    while True:
        # آواز کے حکم کے لیے سنیں
        command = listener.listen_and_transcribe(duration=5)

        if "stop" in command.lower():
            break

        # VLA کے ساتھ ایکشن پلان بنائیں
        actions_json = vla.plan_actions(command)
        print(f"ایکشن پلان: {actions_json}")

        # ایکشنز انجام دیں
        executor.execute_actions(actions_json)

    executor.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

---

## حصہ 4: مکمل VLA سسٹم

### مکمل انٹیگریشن مثال

```python
#!/usr/bin/env python3
"""
گفتگو کرنے والا ہیومنائیڈ روبوٹ
Whisper (ASR) + Gemini (VLA) + ROS 2 (Execution) کو یکجا کرتا ہے
"""

import rclpy
from rclpy.node import Node
import whisper
import google.generativeai as genai
import json
from sensor_msgs.msg import Image
from cv_bridge import CvBridge

class ConversationalRobot(Node):
    def __init__(self):
        super().__init__('conversational_robot')

        # Whisper ماڈل لوڈ کریں
        self.whisper = whisper.load_model("base")

        # Gemini کنفیگر کریں
        genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
        self.gemini = genai.GenerativeModel('gemini-1.5-pro')

        # کیمرہ subscriber
        self.bridge = CvBridge()
        self.camera_sub = self.create_subscription(
            Image,
            '/camera/image_raw',
            self.camera_callback,
            10
        )
        self.latest_image = None

        self.get_logger().info("🤖 گفتگو کرنے والا روبوٹ شروع ہو گیا")

    def camera_callback(self, msg):
        """تازہ ترین کیمرہ فریم محفوظ کریں"""
        self.latest_image = self.bridge.imgmsg_to_cv2(msg, "rgb8")

    def process_voice_command(self, audio_file):
        """مکمل پائپ لائن: آواز → ایکشنز"""

        # 1. تقریر کی شناخت
        result = self.whisper.transcribe(audio_file)
        command = result["text"]
        self.get_logger().info(f"حکم: {command}")

        # 2. VLA پلاننگ (بصارت کے ساتھ)
        prompt = f"""Convert this command to robot actions: {command}

        Available actions: navigate, grasp, place, wave, turn
        Output JSON only."""

        if self.latest_image is not None:
            response = self.gemini.generate_content([prompt, self.latest_image])
        else:
            response = self.gemini.generate_content(prompt)

        actions = json.loads(response.text)

        # 3. ایکشنز انجام دیں
        self.execute_plan(actions)

        return actions

    def execute_plan(self, actions):
        """ایکشن سیکوینس انجام دیں"""
        for action in actions['actions']:
            self.get_logger().info(f"انجام دے رہے ہیں: {action}")
            # مناسب ROS 2 action/service کو کال کریں

def main():
    rclpy.init()
    robot = ConversationalRobot()
    rclpy.spin(robot)

if __name__ == '__main__':
    main()
```

---

## عملی پروجیکٹ: آواز سے کنٹرول شدہ نیویگیشن

### پروجیکٹ کی ضروریات

ایک ہیومنائیڈ روبوٹ بنائیں جو:
1. ✅ آواز کے احکامات سنتا ہے ("Go to the kitchen"، "Wave hello")
2. ✅ اشیاء اور رکاوٹوں کی شناخت کے لیے کیمرہ استعمال کرتا ہے
3. ✅ Gemini VLA استعمال کرتے ہوئے محفوظ نیویگیشن پلان بناتا ہے
4. ✅ Isaac Sim میں حرکتیں انجام دیتا ہے
5. ✅ حقیقی روبوٹ کے لیے Jetson Orin Nano پر ڈیپلائے ہوتا ہے

### تشخیص کے معیار

- آواز کی شناخت کی درستگی > 90%
- VLA درست ایکشن سیکوینس بناتا ہے
- نیویگیشن رکاوٹوں سے بچتا ہے
- ہموار حرکت کی اجرائی
- کامیاب sim-to-real منتقلی

---

## حفاظت کے تحفظات

:::danger اہم حفاظتی قوانین

1. **ایمرجنسی سٹاپ**: ہمیشہ فزیکل e-stop بٹن رکھیں
2. **رفتار کی حدیں**: ٹیسٹنگ کے دوران velocity کو 0.5 m/s پر محدود کریں
3. **ورک سپیس کی حدود**: کوڈ میں محفوظ آپریشن زون کی تعریف کریں
4. **انسانی کا پتہ لگانا**: اگر کوئی شخص 1m رداس میں داخل ہو تو رک جائیں
5. **پہلے سمیولیشن**: حقیقی روبوٹ سے پہلے Isaac Sim میں تمام احکامات ٹیسٹ کریں

:::

### حفاظتی کوڈ کی مثال

```python
class SafetyMonitor(Node):
    def __init__(self):
        super().__init__('safety_monitor')
        self.max_velocity = 0.5  # m/s
        self.safety_radius = 1.0  # میٹر

        # رکاوٹ کا پتہ لگانے کے لیے Laser scan
        self.scan_sub = self.create_subscription(
            LaserScan,
            '/scan',
            self.scan_callback,
            10
        )

    def scan_callback(self, msg):
        """حفاظتی رداس میں رکاوٹوں کی جانچ کریں"""
        min_distance = min(msg.ranges)

        if min_distance < self.safety_radius:
            self.get_logger().warn(f"{min_distance}m پر رکاوٹ - رک رہے ہیں")
            self.emergency_stop()

    def emergency_stop(self):
        """صفر velocity شائع کریں"""
        # تمام حرکت روک دیں
        pass
```

---

## ڈیپلائمنٹ چیک لسٹ

### ڈیپلائمنٹ سے پہلے ٹیسٹ

- [ ] شور والے ماحول میں آواز کی شناخت ٹیسٹ کریں
- [ ] تصدیق کریں کہ VLA محفوظ ایکشنز بناتا ہے
- [ ] ایمرجنسی سٹاپ بٹن ٹیسٹ کریں
- [ ] رکاوٹ کا پتہ لگانا validate کریں
- [ ] بیٹری لیول > 50% کی تصدیق کریں
- [ ] انہی احکامات کے ساتھ سمیولیشن میں ٹیسٹ کریں

### Jetson ڈیپلائمنٹ

```bash
# کوڈ کو Jetson میں کاپی کریں
scp -r conversational_robot/ jetson@192.168.1.100:~/ros2_ws/src/

# Jetson پر بلڈ کریں
ssh jetson@192.168.1.100
cd ~/ros2_ws
colcon build --packages-select conversational_robot
source install/setup.bash

# چلائیں
ros2 launch conversational_robot robot.launch.py
```

---

## وسائل

- [Whisper دستاویزات](https://github.com/openai/whisper)
- [Google Gemini API](https://ai.google.dev/docs)
- [ROS 2 Actions](https://docs.ros.org/en/humble/Tutorials/Beginner-CLI-Tools/Understanding-ROS2-Actions/Understanding-ROS2-Actions.html)
- [Nav2 دستاویزات](https://navigation.ros.org/)
- [VLA تحقیقی مقالات](https://robotics-transformer.github.io/)

---

## مبارک ہو! 🎉

آپ نے فزیکل AI اور ہیومنائیڈ روبوٹکس کورس مکمل کر لیا ہے!

**اب آپ یہ جانتے ہیں:**
- ✅ ہیومنائیڈ روبوٹس کے لیے ROS 2 ایپلیکیشنز بنانا
- ✅ Gazebo اور Isaac Sim میں سمیولیٹ کرنا
- ✅ مصنوعی ڈیٹا کے ساتھ VLA ماڈلز ٹرین کرنا
- ✅ NVIDIA Jetson ہارڈویئر پر ڈیپلائے کرنا
- ✅ آواز سے کنٹرول ہونے والے گفتگو کرنے والے روبوٹس بنانا

**اگلے قدمات:**
- [ROS 2 کمیونٹی](https://discourse.ros.org/) میں شامل ہوں
- [NVIDIA Isaac ROS](https://github.com/NVIDIA-ISAAC-ROS) میں حصہ ڈالیں
- اپنا ہیومنائیڈ روبوٹ پروجیکٹ بنائیں
- اپنا کام [Humanoid Robotics Discord](https://discord.gg/humanoid-robotics) پر شیئر کریں

---

**بناتے رہیں! فزیکل AI کا مستقبل آپ کے ہاتھوں میں ہے۔** 🤖
